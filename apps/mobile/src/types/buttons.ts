export enum ButtonVariants {
  Primary = "primary",
  Secondary = "secondary",
  Error = "error",
  Inline = "inline",
}

export enum ButtonIconsPosition {
  Left = "left",
  Right = "right",
}

export enum ButtonShape {
  Square = "square",
  Circle = "circle",
}
