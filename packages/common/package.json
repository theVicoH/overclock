{
  "name": "common",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0 --ignore-path ../../.gitignore",
    "format": "prettier --config ../config/.prettierrc --write .",
    "common": "pnpm run icons && node generateImport.js",
    "type": "tsc",
    "clear-icons": "node clearIcons.js",
    "icons": "pnpm run clear-icons && svgr --typescript --out-dir src/icons src/assets/icons"
  },
  "devDependencies": {
    "@svgr/cli": "^8.1.0",
    "@svgr/core": "^8.1.0",
    "@types/mock-fs": "^4.13.4",
    "mock-fs": "^5.2.0"
  },
  "eslintConfig": {
    "extends": "../config/.eslintrc.json"
  }
}
